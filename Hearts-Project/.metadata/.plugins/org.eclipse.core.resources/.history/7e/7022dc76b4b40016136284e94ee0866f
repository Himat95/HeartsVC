
public enum Suit {

	SPADES(1), 
	CLUBS(2), 
	DIAMONDS(3), 
	HEARTS(4);
	
	private final int suits; 
	Suit(int suits) {
		this.suits = suits; 
		}
	
	public int getSuitValue() {
		return suits; 
	}
		
}
